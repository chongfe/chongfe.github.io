---
interface Props { en: string; title: string; description: string; }
const { en, title, description } = Astro.props;
---
<div class="page-head"><div class="eyebrow"><span></span>{en}</div><h1>{title}<span class="brand-dot">.</span></h1><p>{description}</p></div>
